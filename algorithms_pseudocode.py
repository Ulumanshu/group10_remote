# This is a sample Python script.

# Press Shift+F10 to execute it or replace it with your code.
# Press Double Shift to search everywhere for classes, files, tool windows, actions, and settings.


def print_hi(name):
    # Task: Suformuluoti mandagu kreipini.
    # Inputs: tik kintamasis name, tipas string
    # Result: mandagus kliento pasveikinimas, vardas turetu visad prasideti is didziosios raides
    # Method: budas, pasiekti tikslui, siuo atveju python string parsing, formatting. (capitalize(), f string
    # Expression: kokius programavimo metodus naudoti?
    print(f'Hi, Mr/Mrs {name.capitalize()}')

# Analysing and testing the algorithm - debug find possible errors
# Evaluating the algorithm effectiveness - measure speed, and ram usage


# Press the green button in the gutter to run the script.
if __name__ == '__main__':
    print_hi('jolanta')

    # Pseudocode example - user login
    """
        If same ip logs in more than 5th time this minute:
            Display Maximum amount of tries reached please wait message.
        Else:
            If username and password password check passes:
                If user device check passes;
                    Redirect the user to the main page with security token attached - Success!
                Else:
                    Redirect the user to Recaptcha robot/human check
            Else:
                Display try again, forgot/password message            
    """

# See PyCharm help at https://www.jetbrains.com/help/pycharm/




